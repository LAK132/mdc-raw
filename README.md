# mdc-raw

an attempt to document and process raw image files from the Minolta RD-175

shell script to convert `*.MDC` to `*.PNG`:
`for i in /some/path/*.MDC; do ./mdc --in-place "$i"; done`

![mdc-raw screenshot](screenshot.png)
